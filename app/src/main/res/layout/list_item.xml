<?xml version="1.0" encoding="utf-8"?>

<!-- This layout is used for both the record list entry, as well as suggestion list entries. -->

<de.tobiasbielefeld.searchbar.ui.ListItem
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/entry"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="horizontal">

    <TextView
        android:id="@+id/text"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:background="?attr/selectableItemBackground"
        android:ellipsize="end"
        android:maxLines="1"
        android:padding="@dimen/record_list_entry_padding"
        android:text=""
        android:textSize="@dimen/record_list_text_size" />

    <ImageView
        android:id="@+id/button"
        android:layout_width="wrap_content"
        android:layout_height="match_parent"
        android:adjustViewBounds="true"
        android:background="?attr/selectableItemBackground"
        android:contentDescription="@string/record_list_button_description"
        android:padding="@dimen/record_list_entry_padding"
        app:srcCompat="@drawable/icon_plus" />

</de.tobiasbielefeld.searchbar.ui.ListItem>